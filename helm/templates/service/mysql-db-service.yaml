# mysql-db-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql-db-service.name }}
  namespace: development
  labels:
    app: {{ .Values.mysql-db-service.name }}
spec:
  ports:
    - port: {{ .Values.mysql-db-service.port }}
      targetPort: {{ .Values.mysql-db-service.port }}
      protocol: TCP
      name: {{ .Values.mysql-db-service.name }}-port
  selector:
    app: {{ .Values.mysql-db-service.name }}
  type: ClusterIP