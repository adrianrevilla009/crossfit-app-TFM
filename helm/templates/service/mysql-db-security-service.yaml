# mysql-db-security-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysqldb.security.service.name }}
  namespace: {{ .Values.namespace.name }}
spec:
  selector:
    app: {{ .Values.mysqldb.security.service.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.mysqldb.security.service.port }}
      targetPort: {{ .Values.mysqldb.security.service.port }}
