apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.api-gateway-microservice.name }}
  namespace: development
  labels:
    app: {{ .Values.api-gateway-microservice.name }}
spec:
  ports:
    - port: {{ .Values.api-gateway-microservice.port }}
      targetPort: {{ .Values.api-gateway-microservice.port }}
      protocol: TCP
      name: {{ .Values.api-gateway-microservice.name }}-port
  selector:
    app: {{ .Values.api-gateway-microservice.name }}
  type: NodePort