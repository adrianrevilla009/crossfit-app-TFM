# mongo-db-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongo-db-service.name }}
  namespace: development
spec:
  selector:
    app: {{ .Values.mongo-db-service.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.mongo-db-service.port }}
      targetPort: {{ .Values.mongo-db-service.port }}
