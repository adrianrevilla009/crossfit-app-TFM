apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.security.microservice.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: {{ .Values.security.microservice.name }}
spec:
  ports:
    - port: {{ .Values.security.microservice.port }}
      targetPort: {{ .Values.security.microservice.port }}
      protocol: TCP
      name: {{ .Values.security.microservice.name }}-port
  selector:
    app: {{ .Values.security.microservice.name }}
  type: ClusterIP