# logstash-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.logstash.service.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.logstash.service.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.logstash.service.name }}
    spec:
      containers:
        - name: {{ .Values.logstash.service.name }}
          image: {{ .Values.logstash.service.image }}
          ports:
            - containerPort: {{ .Values.logstash.service.port }}
          env:
            - name: ELASTICSEARCH_HOST
              value: "elasticsearch"
            - name: ELASTICSEARCH_PORT
              value: "9200"