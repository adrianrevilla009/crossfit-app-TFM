# redis-cache-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.rediscache.service.name }}
  namespace: {{ .Values.namespace.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.rediscache.service.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.rediscache.service.name }}
    spec:
      containers:
        - name: {{ .Values.rediscache.service.name }}
          image: {{ .Values.rediscache.service.image }}
          ports:
            - containerPort: {{ .Values.rediscache.service.port }}
