apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apigateway.microservice.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    app: {{ .Values.apigateway.microservice.name }}
spec:
  ports:
    - port: {{ .Values.apigateway.microservice.port }}
      targetPort: {{ .Values.apigateway.microservice.port }}
      protocol: TCP
      name: {{ .Values.apigateway.microservice.name }}-port
  selector:
    app: {{ .Values.apigateway.microservice.name }}
  type: NodePort