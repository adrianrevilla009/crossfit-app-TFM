apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-security-pvc
  namespace: {{ .Values.namespace.name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
