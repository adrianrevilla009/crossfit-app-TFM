apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-gateway-from-security
  namespace: development
spec:
  podSelector:
    matchLabels:
      app: api-gateway-microservice
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: security-microservice
      ports:
        - port: 8082