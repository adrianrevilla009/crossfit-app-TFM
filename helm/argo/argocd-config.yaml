apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-cm
  namespace: argocd
data:
  repositories: |
    - url: 'https://github.com/adrianrevilla009/crossfit-app-TFM.git'
      type: git
      secret:
          name: argocd-repo-secret
          key: password
