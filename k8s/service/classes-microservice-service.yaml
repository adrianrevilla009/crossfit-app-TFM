apiVersion: v1
kind: Service
metadata:
  name: classes-microservice
  namespace: development
  labels:
    app: classes-microservice
spec:
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: classes-microservice-port
  selector:
    app: classes-microservice
  type: ClusterIP